#include <stdio.h>

/**
 *main - prints the sum of all multiples of 3 or 5 up to 1024
 *multiplies of 3 or 5
 *Return: 0 sucess
 */

int main(void)
{
	int i, z;

	for (i = 1; i < 1024; a++)
	{
	if ((i % 3 == 0) || (i % 5 == 0))
	z += i
	}
	printf("%d\n", z);
	return (0);
}
